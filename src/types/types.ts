// Common types for student app
// TODO: Add specific types as needed

export interface Student {
  id: number;
  telegram_id: number;
  first_name: string;
  last_name: string;
  phone_number: string;
  username?: string;
  photo_url?: string;
}

// Add more types as needed
